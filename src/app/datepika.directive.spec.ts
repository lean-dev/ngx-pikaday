import { DatepikaDirective } from './datepika.directive';

describe('DatepikaDirective', () => {
  it('should create an instance', () => {
    const directive = new DatepikaDirective();
    expect(directive).toBeTruthy();
  });
});
